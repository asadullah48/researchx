apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-researchx
  labels:
    app.kubernetes.io/name: researchx
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8011
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: researchx
